name: contextdb
description: ContextDB epistemic memory provider for Hermes (credibility, sources, narrative retrieval)
version: 0.1.0
author: antiartificial
entrypoint: __init__.py
cli: cli.py
requires:
  - contextdb >= 0.1.0
config:
  namespace: hermes-agent
  mode: agent_memory